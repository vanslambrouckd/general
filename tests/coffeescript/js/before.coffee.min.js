(function() {
  var after, around, before, sale, withAuditLog,
    __slice = [].slice;

  console.clear;

  sale = function(user, product) {
    var auditLog;
    return auditLog = "Sold " + product + " to " + user;
  };

  before = function(decoration) {
    (function(base) {
      return function() {
        var params;
        params = 1 <= arguments.length ? __slice.call(arguments, 0) : [];
      };
    });
    decoration.apply(null, params);
    return base.apply(null, params);
  };

  withAuditLog = before(function() {
    var params;
    params = 1 <= arguments.length ? __slice.call(arguments, 0) : [];
    return auditLogs.apply(null, params);
  });

  after = function(decoration) {
    var result;
    (function(base) {
      return function() {
        var params;
        params = 1 <= arguments.length ? __slice.call(arguments, 0) : [];
      };
    });
    result = base.apply(null, params);
    decoration.apply(null, params);
    return result;
  };

  around = function(decoration) {};

}).call(this);
